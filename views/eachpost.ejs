<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,900" rel="stylesheet">
    
    <link rel="stylesheet" type="text/css" href="/scss/navbar.css">
    <link rel="stylesheet" type="text/css" href="/scss/homepage.css">
    <title>Post </title>


    <style>

        body {
            font-family: Arial, Helvetica, sans-serif; 


            letter-spacing: 1px;
            word-spacing: 3px;
        }
        .post {
            padding: 1.5%;
            /* background: green; */
        }

        .post__title {
            /* background: pink; */
            width: 80%;
            padding: 10px;
            margin: auto;
            text-align: center;
        }

        .post__title p {
            font-size: 250%;
            font-weight: 700;
        }

        .nameNdate {
            /* background: red; */
            display: flex;
            justify-content: space-between;
            margin-top: .2%;
            margin-bottom: .6%;
            /* padding: 10px; */
        }

        .name {
            font-weight: 300;
        }

        img {
            margin: auto;
            text-align: center;
        }

        .buttons {
            text-align: center;
            width: 100%;
            display: flex;
            margin: 1% 0 1.5% 0;
        }
        .buttons * {
            width: 50%;
            text-transform: uppercase;
            outline: none;
            border: none;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(to right, rgb(138, 202, 140), rgb(81, 172, 84), #4CAF50,  #4CAF50, rgb(138, 202, 140));
            font-weight: 500;
            text-decoration: none;
            display: inline-block;
            padding: 15px;
            color: #FFFFFF;
            font-size: 24px;
            letter-spacing: 2px;
            word-spacing: 4px;
            cursor: pointer;
            transition: color .3s ease-in;
        }
        .EDIT {
            border-right: 3px solid rgb(92, 139, 70); 
        }

        .EDIT:hover, .DELETE:hover {
            outline: none;
            text-decoration: none;
            color: black;
        }
    </style>

</head>
<body>
    <% include ./partials/navbar %>
    <% include ./partials/messages %>

    <section class="post">
        <div class="post__title">
            <p> <%= article.postTitle %> </p>
        </div>

        <div class="nameNdate">
            <div class="name">
                <%= article.username %>
            </div>
            <div class="date">
                <%= article.date %>
            </div>
        </div>
        
        <% if(userID.toString().trim() === article.userID.toString().trim()) { %>
            <div class="buttons">
                <a href = "#" class="EDIT"> EDIT POST </a>
                <a href = "#" class="DELETE"> DELETE POST </a>
            </div>
        <% } %>

        <div class="content">
                <%- article.postContent %> 
        </div>
    </section>

    <% include ./partials/disappearFlask %>
</body>
</html>

